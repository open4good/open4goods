---
description: Fix the frontend dev server and configure debugging
---
1. Fix the `vite-plugin-vue-mcp` error in `nuxt.config.ts`.
2. Verify `package.json` dependencies if needed.
3. Ensure `.vscode/launch.json` is correctly configured for frontend debugging.
4. Verify the dev server starts correctly.
