name: ✅ CI - PR Check with Maven Build

on:
  pull_request:
    branches:
      - 'main'
    paths-ignore:
      - 'frontend/**'


jobs:
  test:
    name: PR Check - Maven Build
    uses: ./.github/workflows/maven-build.yml
    with:
      runner: self-hosted
