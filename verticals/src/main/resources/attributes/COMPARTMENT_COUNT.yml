# ============================================================================
# COMPARTMENT_COUNT — Compartment count (Nombre de compartiments)
# Maintained: 2026-05-27 by attributes-maintenance prompt (batch 001)
# Used in verticals: (unused)
# Type: NUMERIC
# Coverage snapshot: N/A — no vertical uses this attribute
# Taxonomy coverage: icecat ❌  eprel ❌  etim ❌  wikidata ❌
# Purpose: Number of storage compartments; currently unused across all verticals.
# ============================================================================

key: "COMPARTMENT_COUNT"
faIcon: "mdi-view-grid-outline"
name:
  default: "Compartment count"
  fr: "Nombre de compartiments"
filteringType: "NUMERIC"
asScore: false

synonyms:
  all:
    - "NUMBERCOMPARTMENTS"
parser:
  normalize: true
  trim: true
  lowerCase: false
  upperCase: true
  removeParenthesis: false
