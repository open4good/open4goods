# ============================================================================
# DISPOSITIF_DE_SECURITE — Security features (Dispositif de sécurité)
# Maintained: 2026-05-27 by attributes-maintenance prompt (batch 001)
# Used in verticals: oven
# Type: TEXT
# Coverage snapshot: prod legacy endpoint returned no matching canonical-key stats
# Taxonomy coverage: icecat ✅  eprel ❌  etim ❌  wikidata ❌
# Purpose: Safety mechanisms present on the appliance (oven); e.g. child lock,
#          door cooling, residual heat indicator — key for buyer safety filtering.
# ============================================================================

key: "DISPOSITIF_DE_SECURITE"
faIcon: "mdi-lock-outline"
name:
  default: "Security features"
  fr: "Dispositif de sécurité"
filteringType: "TEXT"
asScore: false

referentials:
  icecat:
    - featureId: 248
      name: "Security features"

synonyms:
  all:
    - "DISPOSITIF DE SECURITE"
    - "SECURITE"
    - "PORTE FROIDE"
    - "SECURITY FEATURES"
parser:
  normalize: true
  trim: true
  lowerCase: false
  upperCase: true
  removeParenthesis: false
