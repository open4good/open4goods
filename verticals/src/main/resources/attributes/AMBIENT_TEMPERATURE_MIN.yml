# ============================================================================
# AMBIENT_TEMPERATURE_MIN — Minimum ambient temperature (Température ambiante minimale)
# Maintained: 2026-05-24 by attributes-maintenance prompt (batch 000)
# Used in verticals: refrigerator
# Type: NUMERIC
# Coverage snapshot: insufficient data from prod API
# Taxonomy coverage: icecat ✅  eprel ❌  etim ❌  wikidata ❌
# Purpose: Minimum ambient temperature at which the appliance is rated to
#          operate correctly; used to filter by climate suitability.
# ============================================================================

key: "AMBIENT_TEMPERATURE_MIN"
faIcon: "mdi-thermometer-low"
name:
  default: "Minimum ambient temperature"
  fr: "Température ambiante minimale"
suffix:
  default: "°C"
  fr: "°C"
filteringType: "NUMERIC"
asScore: false

referentials:
  icecat:
    - featureId: 19610
      name: "Ambient temperature (min)"

synonyms:
  all:
    - "MINAMBIENTTEMP"
    - "MINIMUM AMBIENT TEMPERATURE"
    - "TEMPÉRATURE AMBIANTE MINIMALE"
    - "TEMPERATURE AMBIANTE MINIMALE"
parser:
  normalize: true
  trim: true
  lowerCase: false
  upperCase: true
  removeParenthesis: false
  deleteTokens:
    - "°C"
    - "C"
