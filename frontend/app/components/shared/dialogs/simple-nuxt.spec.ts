import { test, expect } from 'vitest'

// Simple test to verify that @nuxt/test-utils is configured correctly
test('runs a basic @nuxt/test-utils assertion', async () => {
  // Basic expectation that should always succeed
  expect(true).toBe(true)
})
