server:
  port: 8082

spring:
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: https://auth.example.com/issuer

springdoc:
  api-docs:
    path: /v3/api-docs

  swagger-ui:
    use-root-path: true

front:
  security:
    enabled: false
    cors-allowed-hosts: 
      "http://localhost:8082"
    
xwiki:
    username: nudger
    password: nudger 
    baseUrl: https://wiki.nudger.fr
        
