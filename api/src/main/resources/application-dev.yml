
server:
  port: 8081


logging:
    level:
      ROOT: warn
      org.open4goods: warn
      GENERIC_LOGGER: error

# The key to provide to get ADMIN_ROLE
adminKey: open4goods

  
crawlerKeys:
  - open4goods

verticalsFolder: "/opt/open4goods/config/verticals/"
datasourcesfolder: "/opt/open4goods/config/datasources/"
dedicatedLoggerToConsole: true

spring:
  thymeleaf:
      cache: false


######################################
# The locally embeded crawler node
#####################################        
fetcherProperties:
  apiKey: open4goods
  concurrentFetcherTask: 2
  apiSynchConfig:
    nodeName: localNode
    nodeUrl: http://localhost:8081  


elasticsearch.username: 
elasticsearch.password:  
elasticsearch.host: 127.0.0.1

xwiki-o4g:
  baseUrl: = https://wiki.nudger.fr
  auth:
    password: couilledu22
    username: Tif