spring:
  ai:
    openai:
        api-key: ${OPENAI_API_KEY:test-key-not-set}
        chat:
          api-key: ${OPENAI_API_KEY:test-key-not-set}
            
gen-ai-config:
  cache-templates: true
  enabled: true
  prompts-templates-folder: src/test/resources/prompts
  
  