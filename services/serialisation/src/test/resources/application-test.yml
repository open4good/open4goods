urlfetcher:
  domains:
    "localhost":
      userAgent: "TestAgent/1.0"
      strategy: "PLAYWRIGHT"
      customHeaders:
        Accept: "application/json"
      timeout: 5000
      retryPolicy:
        maxRetries: 1
        delayBetweenRetries: 500
