package org.open4goods.model.rating;
public enum RatingType {
	// The global is the one computed from others
	SITES, TECHNICAL, RSE, COMMENT, TECHNICAL_SPEC, SCORE,
	/** When the rating comes from attribute conversion**/
	FROM_ATTRIBUTE

}